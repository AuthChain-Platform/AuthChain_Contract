// SPDX-License-Identifier: MIT
pragma solidity 0.8.27;

contract ProductTracker {
   
   
    // struct ProductHistoryEntry {
    //     uint256 timestamp;
    //     address owner;
    //     string status;
    // }

    // mapping(uint256 => ProductHistoryEntry[]) public productHistory;

    // event ProductHistoryUpdated(uint256 indexed productCode, address indexed owner, string indexed status);

    // function addProductHistory(uint256 productCode, address owner, string memory status) public {
    //     productHistory[productCode].push(ProductHistoryEntry({
    //         timestamp: block.timestamp,
    //         owner: owner,
    //         status: status
    //     }));

    //     emit ProductHistoryUpdated(productCode, owner, status);
    // }

    // function getProductHistory(uint256 productCode) public view returns (ProductHistoryEntry[] memory) {
    //     return productHistory[productCode];
    // }
}